{
  "name": "sketchy-blobatar-workspace",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*", "apps/*"],
  "scripts": {
    "dev": "bun --filter demo dev",
    "site": "bun --filter site dev",
    "build:site": "bun --filter site build",
    "test": "bun --filter sketchy-blobatar test",
    "size": "bun --filter sketchy-blobatar size",
    "check": "bun --filter '*' check"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^7.0.2"
  }
}
